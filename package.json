{
  "name": "webApps",
  "productName": "WebApps",
  "version": "1.0.0",
  "description": "web apps",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager . WebApps --platform=darwin --arch=x64 --icon=./images/cookie --overwrite"
  },
  "author": "homfen@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.8.6",
    "electron-packager": "^12.0.2"
  },
  "dependencies": {
    "electron-tabs": "^0.9.1",
    "jquery": "^3.3.1"
  }
}
